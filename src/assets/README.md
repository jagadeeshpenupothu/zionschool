Use this folder for Astro-imported assets such as icons, optimized local images, or component-specific media.

This starter keeps editable placeholder media in `public/assets/images` so JSON data can reference simple public paths that are easy to migrate into Decap CMS.
