---
const { eyebrow, title, text, align = "center" } = Astro.props;
---

<div class={`section-header ${align === "left" ? "section-header-left" : ""}`}>
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  {title && <h2>{title}</h2>}
  {text && <p>{text}</p>}
</div>
