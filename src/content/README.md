This folder is reserved for future Decap CMS collections.

Recommended future collections:

- `courses`
- `events`
- `staff`
- `gallery`
- `testimonials`
- `pages`

The current starter uses `src/data/*.json` to keep content structured and easy to migrate.
